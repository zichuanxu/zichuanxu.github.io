---
title: "322. 零钱兑换 (Coin Change)"
date: 2026-06-16
category: algorithms
topic: dp
number: 322
difficulty: 中等
tags: [广度优先搜索, 数组, 动态规划]
leetcode_url: "https://leetcode.cn/problems/coin-change/"
permalink: /leetcode/0322-coin-change/
layout: leetcode-post
---

## 题目描述

给定不同面额硬币 `coins` 和总金额 `amount`，返回凑成该金额所需的最少硬币数；无法凑出返回 `-1`。每种硬币数量无限。

**示例：** `coins = [1,2,5], amount = 11` → `3`（`5+5+1`）。

## 思路分析

**核心思想：** 完全背包 DP。`dp[i]` = 凑出金额 `i` 的最少硬币数；对每个金额枚举所有硬币转移 `dp[i] = min(dp[i], dp[i-c] + 1)`。

**详细步骤：**

1. `dp[0] = 0`，其余初始化为「不可达」（极大值）。
2. 对每个金额 `i`，遍历硬币 `c ≤ i` 取最小。
3. 末值仍为极大值则返回 `-1`。

## 代码实现

<div class="code-tabs">
<div class="tab-buttons">
<button class="tab-btn" data-lang="go" onclick="switchCodeTab(this)">Go</button>
<button class="tab-btn" data-lang="python" onclick="switchCodeTab(this)">Python</button>
</div>
<div class="tab-content" data-lang="go" markdown="1">

```go
func coinChange(coins []int, amount int) int {
    const inf = 1 << 30
    dp := make([]int, amount+1)
    for i := 1; i <= amount; i++ {
        dp[i] = inf
        for _, c := range coins {
            if c <= i && dp[i-c]+1 < dp[i] {
                dp[i] = dp[i-c] + 1
            }
        }
    }
    if dp[amount] == inf {
        return -1
    }
    return dp[amount]
}
```

</div>
<div class="tab-content" data-lang="python" markdown="1">

```python
class Solution:
    def coinChange(self, coins: List[int], amount: int) -> int:
        dp = [0] + [float("inf")] * amount
        for i in range(1, amount + 1):
            for c in coins:
                if c <= i:
                    dp[i] = min(dp[i], dp[i - c] + 1)
        return dp[amount] if dp[amount] != float("inf") else -1
```

</div>
</div>

## 复杂度分析

| | 时间复杂度 | 空间复杂度 |
|---|---|---|
| Go / Python | O(amount·len(coins)) | O(amount) |

**解释：** 双层循环：金额 × 硬币种类。
