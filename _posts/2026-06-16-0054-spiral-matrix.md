---
title: "54. 螺旋矩阵 (Spiral Matrix)"
date: 2026-06-16
category: algorithms
topic: matrix
number: 54
difficulty: 中等
tags: [数组, 矩阵, 模拟]
leetcode_url: "https://leetcode.cn/problems/spiral-matrix/"
permalink: /leetcode/0054-spiral-matrix/
layout: leetcode-post
---

## 题目描述

给定 `m × n` 矩阵，按顺时针螺旋顺序返回所有元素。

**示例：** `[[1,2,3],[4,5,6],[7,8,9]]` → `[1,2,3,6,9,8,7,4,5]`。

## 思路分析

**核心思想：** 维护上下左右四条边界，按「右→下→左→上」依次收缩边界遍历。

**详细步骤：**

1. `top/bottom/left/right` 初始为四条边界。
2. 每轮：上行从左到右、右列从上到下、（若还有行）下行从右到左、（若还有列）左列从下到上。
3. 每遍历一条边就收缩对应边界，直到边界交错。

## 代码实现

<div class="code-tabs">
<div class="tab-buttons">
<button class="tab-btn" data-lang="go" onclick="switchCodeTab(this)">Go</button>
<button class="tab-btn" data-lang="python" onclick="switchCodeTab(this)">Python</button>
</div>
<div class="tab-content" data-lang="go" markdown="1">

```go
func spiralOrder(matrix [][]int) []int {
    if len(matrix) == 0 {
        return nil
    }
    top, bottom := 0, len(matrix)-1
    left, right := 0, len(matrix[0])-1
    res := []int{}
    for top <= bottom && left <= right {
        for j := left; j <= right; j++ {
            res = append(res, matrix[top][j])
        }
        top++
        for i := top; i <= bottom; i++ {
            res = append(res, matrix[i][right])
        }
        right--
        if top <= bottom {
            for j := right; j >= left; j-- {
                res = append(res, matrix[bottom][j])
            }
            bottom--
        }
        if left <= right {
            for i := bottom; i >= top; i-- {
                res = append(res, matrix[i][left])
            }
            left++
        }
    }
    return res
}
```

</div>
<div class="tab-content" data-lang="python" markdown="1">

```python
class Solution:
    def spiralOrder(self, matrix: List[List[int]]) -> List[int]:
        top, bottom = 0, len(matrix) - 1
        left, right = 0, len(matrix[0]) - 1
        res = []
        while top <= bottom and left <= right:
            for j in range(left, right + 1):
                res.append(matrix[top][j])
            top += 1
            for i in range(top, bottom + 1):
                res.append(matrix[i][right])
            right -= 1
            if top <= bottom:
                for j in range(right, left - 1, -1):
                    res.append(matrix[bottom][j])
                bottom -= 1
            if left <= right:
                for i in range(bottom, top - 1, -1):
                    res.append(matrix[i][left])
                left += 1
        return res
```

</div>
</div>

## 复杂度分析

| | 时间复杂度 | 空间复杂度 |
|---|---|---|
| Go / Python | O(m·n) | O(1) |

**解释：** 每个元素恰好访问一次；输出数组不计额外空间。
