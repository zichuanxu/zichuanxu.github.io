---
title: "79. 单词搜索 (Word Search)"
date: 2026-06-16
category: algorithms
topic: backtracking
number: 79
difficulty: 中等
tags: [深度优先搜索, 数组, 字符串, 回溯, 矩阵]
leetcode_url: "https://leetcode.cn/problems/word-search/"
permalink: /leetcode/0079-word-search/
layout: leetcode-post
---

## 题目描述

给定 `m × n` 字符网格 `board` 和字符串 `word`，判断 `word` 是否能由网格中相邻（上下左右）且不重复使用的字母连成。

**示例：** 在网格中查找 `"ABCCED"` → `true`。

## 思路分析

**核心思想：** DFS 回溯。从每个格子尝试匹配 `word[0]`，沿四方向匹配下一字符；访问中的格子临时改写以避免重用，回溯时复原。

**详细步骤：**

1. 对每个起点调用 `dfs(i, j, k=0)`。
2. 越界或字符不匹配返回 `false`；`k` 到末尾返回 `true`。
3. 暂存并标记当前格为 `#`，四向递归 `k+1`；结束后复原。

## 代码实现

<div class="code-tabs">
<div class="tab-buttons">
<button class="tab-btn" data-lang="go" onclick="switchCodeTab(this)">Go</button>
<button class="tab-btn" data-lang="python" onclick="switchCodeTab(this)">Python</button>
</div>
<div class="tab-content" data-lang="go" markdown="1">

```go
func exist(board [][]byte, word string) bool {
    m, n := len(board), len(board[0])
    var dfs func(i, j, k int) bool
    dfs = func(i, j, k int) bool {
        if i < 0 || i >= m || j < 0 || j >= n || board[i][j] != word[k] {
            return false
        }
        if k == len(word)-1 {
            return true
        }
        tmp := board[i][j]
        board[i][j] = '#' // 标记访问中
        found := dfs(i+1, j, k+1) || dfs(i-1, j, k+1) ||
            dfs(i, j+1, k+1) || dfs(i, j-1, k+1)
        board[i][j] = tmp // 回溯
        return found
    }
    for i := 0; i < m; i++ {
        for j := 0; j < n; j++ {
            if dfs(i, j, 0) {
                return true
            }
        }
    }
    return false
}
```

</div>
<div class="tab-content" data-lang="python" markdown="1">

```python
class Solution:
    def exist(self, board: List[List[str]], word: str) -> bool:
        m, n = len(board), len(board[0])
        def dfs(i, j, k):
            if i < 0 or i >= m or j < 0 or j >= n or board[i][j] != word[k]:
                return False
            if k == len(word) - 1:
                return True
            tmp, board[i][j] = board[i][j], '#'
            found = (dfs(i + 1, j, k + 1) or dfs(i - 1, j, k + 1) or
                     dfs(i, j + 1, k + 1) or dfs(i, j - 1, k + 1))
            board[i][j] = tmp
            return found
        for i in range(m):
            for j in range(n):
                if dfs(i, j, 0):
                    return True
        return False
```

</div>
</div>

## 复杂度分析

| | 时间复杂度 | 空间复杂度 |
|---|---|---|
| Go / Python | O(m·n·3^L) | O(L) |

**解释：** `L` 为单词长度，每步最多 3 个新方向；递归深度 O(L)。
