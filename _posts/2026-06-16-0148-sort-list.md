---
title: "148. 排序链表 (Sort List)"
date: 2026-06-16
category: algorithms
topic: linked-list
number: 148
difficulty: 中等
tags: [链表, 双指针, 分治, 排序, 归并排序]
leetcode_url: "https://leetcode.cn/problems/sort-list/"
permalink: /leetcode/0148-sort-list/
layout: leetcode-post
---

## 题目描述

给定链表头节点 `head`，将其按升序排序后返回。要求 O(n log n) 时间。

**示例：** `4→2→1→3` → `1→2→3→4`。

## 思路分析

**核心思想：** 自顶向下**归并排序**。快慢指针找中点断开成两半，分别递归排序，再合并两个有序链表。

**详细步骤：**

1. 边界：空或单节点直接返回。
2. 快慢指针找中点，断开为左右两段。
3. 递归排序左右，再用「合并两个有序链表」拼接。

## 代码实现

<div class="code-tabs">
<div class="tab-buttons">
<button class="tab-btn" data-lang="go" onclick="switchCodeTab(this)">Go</button>
<button class="tab-btn" data-lang="python" onclick="switchCodeTab(this)">Python</button>
</div>
<div class="tab-content" data-lang="go" markdown="1">

```go
func sortList(head *ListNode) *ListNode {
    if head == nil || head.Next == nil {
        return head
    }
    slow, fast := head, head.Next
    for fast != nil && fast.Next != nil {
        slow = slow.Next
        fast = fast.Next.Next
    }
    mid := slow.Next
    slow.Next = nil // 断开
    left, right := sortList(head), sortList(mid)
    dummy := &ListNode{}
    tail := dummy
    for left != nil && right != nil {
        if left.Val <= right.Val {
            tail.Next, left = left, left.Next
        } else {
            tail.Next, right = right, right.Next
        }
        tail = tail.Next
    }
    if left != nil {
        tail.Next = left
    } else {
        tail.Next = right
    }
    return dummy.Next
}
```

</div>
<div class="tab-content" data-lang="python" markdown="1">

```python
class Solution:
    def sortList(self, head: Optional[ListNode]) -> Optional[ListNode]:
        if not head or not head.next:
            return head
        slow, fast = head, head.next
        while fast and fast.next:
            slow = slow.next
            fast = fast.next.next
        mid, slow.next = slow.next, None
        left, right = self.sortList(head), self.sortList(mid)
        dummy = tail = ListNode()
        while left and right:
            if left.val <= right.val:
                tail.next, left = left, left.next
            else:
                tail.next, right = right, right.next
            tail = tail.next
        tail.next = left or right
        return dummy.next
```

</div>
</div>

## 复杂度分析

| | 时间复杂度 | 空间复杂度 |
|---|---|---|
| Go / Python | O(n·log n) | O(log n) |

**解释：** 归并排序的经典复杂度；空间为递归栈深度。
